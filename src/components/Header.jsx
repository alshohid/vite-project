  
 
 const Header = (props) => {
    return (
        <div className="bg-info h-50 text-center ">
            <h1 className=' text-secondary-emphasis'>{props.title} </h1>
        </div>
    );
 };
 
 export default Header;